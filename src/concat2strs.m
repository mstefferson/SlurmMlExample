function c = concat2strs(a, b)
% c = concat2strs(a, b)
% Concatenate two strings
%
% Inputs:
% a (str)
%   First str to concatenate
% b (str)
%   Second str to concatenate
%
% Outputs:
% c (str)
%   Concatenated str of inputs

c = [a b];
